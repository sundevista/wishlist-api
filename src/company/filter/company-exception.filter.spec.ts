import { CompanyExceptionFilter } from './company-exception.filter';

describe('CompanyExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CompanyExceptionFilter()).toBeDefined();
  });
});
