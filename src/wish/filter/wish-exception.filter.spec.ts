import { WishExceptionFilter } from './wish-exception.filter';

describe('WishExceptionFilter', () => {
  it('should be defined', () => {
    expect(new WishExceptionFilter()).toBeDefined();
  });
});
